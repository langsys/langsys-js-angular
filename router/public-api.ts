/**
 * langsys-js-angular/router — wiring between the Angular Router and the base SDK.
 *
 * A separate entry point so that applications without `@angular/router` never import it.
 */
export { provideLangsysNavigation } from './provide-langsys-navigation';
